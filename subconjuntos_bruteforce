for(int mask=0;mask< (1<<n);mask++){
        for(int bit =0;bit<n;bit++){
            if(mask & 1<<bit){
                //opera no subconjunto
            }
        }
        //aqui fala sobre o subconjunto, pois já "colocou" todos
    }
